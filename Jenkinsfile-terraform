pipeline {
    agent any
    stages{
        stage('Git Checkout'){
        steps {
        echo 'cloning'
          }
        }
        stage('Terraform Init'){
            steps{
                sh 'echo "Hello1"'
            }
        }
        stage('Terraform Apply'){
            steps{
                
                echo "${currentBuild.currentResult}"
            }
        }
    }
        post {
         success {
         echo "${currentBuild.currentResult}"
         }
        }
       
}
