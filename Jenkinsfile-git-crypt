node {
    stages {
        stage(git-crypt) 
        withCredentials([
            (credintialid)
        ])
        {
          echo "***start of pipline***"
          sh """

          """
        }
    }
}
